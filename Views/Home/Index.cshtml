<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Contract Monthly Claim System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: #f8f9fa;
        }

        .feature-card, .role-card {
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            transition: transform 0.2s, box-shadow 0.2s;
        }

            .feature-card:hover, .role-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 20px rgba(0,0,0,0.15);
            }

            .feature-card i, .role-card i {
                font-size: 2rem;
                margin-bottom: 10px;
            }

        .role-card {
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: block;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="text-center mb-5">
            <i class="bi bi-mortarboard-fill text-primary" style="font-size:3rem;"></i>
            <h1 class="fw-bold mt-2">Contract Monthly Claim System</h1>
            <p class="text-muted">Streamlined claim submission, review, and approval process for academic institutions</p>
        </div>

        <!-- Features -->
        <div class="row text-center mb-5 g-4">
            <div class="col-md-4">
                <div class="feature-card bg-light">
                    <i class="bi bi-clock-history text-primary"></i>
                    <h5 class="mt-2">Quick Submission</h5>
                    <p class="text-muted">Lecturers can submit claims with one-click simplicity</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card bg-light">
                    <i class="bi bi-check2-circle text-success"></i>
                    <h5 class="mt-2">Transparent Tracking</h5>
                    <p class="text-muted">Real-time status updates throughout the approval process</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card bg-light">
                    <i class="bi bi-mortarboard-fill text-warning"></i>
                    <h5 class="mt-2">Multi-Level Approval</h5>
                    <p class="text-muted">Coordinated review by Programme Coordinators and Academic Managers</p>
                </div>
            </div>
        </div>

        <!-- Login Button -->
        <div class="text-center">
            <a href="@Url.Action("Index", "Login")" class="btn btn-primary btn-lg px-5">Login</a>
        </div>
    </div>
</body>
</html>
